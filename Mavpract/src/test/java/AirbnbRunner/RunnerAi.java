package AirbnbRunner;

import org.openqa.selenium.WebDriver;
import org.openqa.selenium.chrome.ChromeDriver;
import org.openqa.selenium.support.ui.WebDriverWait;

import io.cucumber.testng.CucumberOptions;

/*@RunnerClass(Cucumber.class)
@CucumberOptions(features="",
glue="",
plugin="pretty","html:html/reports.html")

public class RunnerAi {
	
	WebDriver driver=new ChromeDriver();
	

	driver.switch.Alert.dismiss();
	driver.switch.Alert.Accept();
	driver.switchTo().Alert.getText();
	driver.switchTo.Alert.SendKeys();
	
	Robot r=new Robot();
	r.KeyPress(KeyEvent.Vk_Down);
	r.KeyPress(KeyEvent.VK_Up);
	r.Keypress(KeyEvent.VK_Tab);
	r.KeyPress(KeyEvent,VK_Enter);
	
	r.mousepress(inputEvent.BUTTON1.DOWN_MASK);
	r.mousemove(630,430);
	r.mouserelease(inputevent.BUTTON1.DOWN_MASK);
	
	
	driver.getWindowhandles();
		
	driver.manage().timeout.implicitwait(10,timeout.milliseconds);
	
	@SuppressWarnings("deprecation")
	WebDriverWait wb= new WebDriverWait(driver,30);
	wait.(until.expectedConditionOf.ElementLocatedby.xpath)
}
*/
